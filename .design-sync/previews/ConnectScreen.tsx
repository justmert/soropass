import { ConnectScreen } from '@soropass/ui-react';

/**
 * The entry chooser — a stateless fork between creating a brand-new passkey
 * wallet and connecting an existing passkey. One canonical look.
 */
export const Default = () => <ConnectScreen />;
