{
  "name": "@stellar-passkey/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reference demo for the headless (S18) + styled (S20) passkey UI. Mock or virtual-authenticator backed; zero network.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@stellar-passkey/core": "workspace:^",
    "@stellar-passkey/ui": "workspace:^"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vite": "^5.4.0"
  }
}
