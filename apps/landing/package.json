{
  "name": "@soropass/landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "soropass.dev landing — what it is, a live testnet passkey demo, on-chain proof, and links to the docs.",
  "scripts": {
    "dev": "node scripts/copy-skill.mjs && vite",
    "build": "node scripts/copy-skill.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@noble/curves": "^2.4.0",
    "@noble/hashes": "^2.4.0",
    "@soropass/core": "workspace:^",
    "@soropass/ui": "workspace:^",
    "@stellar/stellar-sdk": "^15.1.0",
    "buffer": "^6.0.3",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.4.0"
  }
}
