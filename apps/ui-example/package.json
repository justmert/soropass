{
  "name": "@soropass/ui-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Consuming example for @soropass/ui: the drop-in passkey screens wired to the mock kit, no framework.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@soropass/core": "workspace:^",
    "@soropass/ui": "workspace:^"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.1.7"
  }
}
