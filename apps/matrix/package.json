{
  "name": "@stellar-passkey/matrix",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "VitePress compatibility-matrix + docs site. Implemented in S05–S10, S22.",
  "scripts": {
    "dev": "echo \"matrix site — implemented in S09 (YK-435)\" && exit 0",
    "matrix:pull": "tsx scripts/pull-bcd.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@mdn/browser-compat-data": "^8.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.22.3",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
